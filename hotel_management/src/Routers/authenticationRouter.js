// // const verifyToken = require("../Middlewares/authentication/authentication");
// const passport = require("passport");
// require("../config/passport");
// require("../Middlewares/authentication/authentication");
// // const passport = require("../Middlewares/authentication/passport")
// const {
//   registerUser,
//   loginUser,
//   myDetails,
// } = require("../Controllers/authController/authenticationController");
// const authenticationRoute = require("express").Router();

// authenticationRoute.post("/register", registerUser);
// authenticationRoute.post("/login", passport.authenticate("jwt", {session:false}), loginUser);
// authenticationRoute.get("/mydetails", passport.authenticate("jwt", {session:false}), myDetails);


// module.exports = authenticationRoute;
